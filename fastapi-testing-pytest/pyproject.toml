[project]
name = "fastapi-testing-pytest"
version = "0.1.0"
description = "Runnable FastAPI API testing example using pytest and TestClient"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
]

[dependency-groups]
dev = [
    "httpx2>=2.12.0",
    "pytest>=9.1.1",
]
